#pragma once
#include <vector>
#include <iostream>
#include <string>
using namespace std;
class View
{
public:
	void displayVector(vector<string>& vec);
	void writeline(string message);
};
